# Host-authoritative listen server networking

Matches will use a host-authoritative listen server: one player's machine hosts the simulation and other clients connect by manual IP and port. This accepts host advantage, host disconnect failure, and LAN or port-forwarding assumptions in exchange for avoiding dedicated infrastructure, matchmaking, relays, and deterministic peer lockstep complexity.
